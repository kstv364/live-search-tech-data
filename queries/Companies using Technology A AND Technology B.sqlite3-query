-- database: ./builtwith.db

SELECT c.root_domain
FROM company c
WHERE EXISTS (
  SELECT 1 FROM v_company_tech v
  WHERE v.company_id = c.id AND v.tech_name = 'PayPal'
)
AND EXISTS (
  SELECT 1 FROM v_company_tech v
  WHERE v.company_id = c.id AND v.tech_name = 'ASP.NET'
);

